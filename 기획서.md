# 서비스 기획서 — AI 여행 추천 미니 서비스

## 1. 서비스 목적

여행 날짜만 입력하면 AI가 그 시기에 어울리는 국내 여행지와 주변 맛집을 즉시 추천해주는
웹 서비스. "어디로 여행 갈지 고민하는 시간"을 줄여주는 것이 핵심 가치다.

## 2. 타겟 사용자

- 국내 여행지를 정하지 못해 고민하는 20~30대 개인 여행객
- 짧은 시간 안에 "지금 이 시기에 어디가 좋을지" 아이디어만 빠르게 얻고 싶은 사람
- 별도 앱 설치 없이 브라우저에서 바로 써보고 싶은 사용자

## 3. 페이지 구성 (3개, 상단 네비게이션으로 이동)

| 페이지 | 경로 | 내용 |
|---|---|---|
| 홈 | `index.html` | 서비스 소개, 핵심 가치, "여행 추천 받으러 가기" CTA |
| 여행 추천 (AI 기능) | `recommend.html` | 날짜 입력 폼 → AI 추천 결과(지역/날씨/행사/맛집) 출력 |
| 소개/사용법 | `about.html` | 서비스가 어떻게 동작하는지, 사용 방법, 기술 스택 소개 |

메뉴 이동은 모든 페이지 상단에 고정된 네비게이션 바(로고 + 3개 메뉴 링크)로 제공하며,
모바일 폭에서는 메뉴가 세로로 쌓이는 방식으로 반응형 처리한다.

## 4. 핵심 기능

1. **네비게이션**: 3개 페이지를 자유롭게 오갈 수 있는 상단 메뉴 (모든 페이지 공통)
2. **AI 여행 추천 (핵심 기능)**: `recommend.html`에서 날짜를 입력하면
   - 프론트가 `fetch('/api/recommend', {method:'POST', body:{date}})` 호출
   - 백엔드(Vercel Serverless Function, Python)가 OpenAI로 추천 지역/날씨/행사/이유를
     JSON으로 생성하고, Kakao Local API로 그 지역 맛집을 검색해 함께 반환
   - 프론트는 응답을 카드 형태로 화면에 렌더링
3. **반응형 레이아웃**: 데스크톱/태블릿/모바일 3가지 폭에서 레이아웃이 깨지지 않음

## 5. AI 기능 입력/출력/실패 처리 기준

**입력**
- `date` (YYYY-MM-DD, HTML `<input type="date">`로 형식 자체를 강제)

**출력** (성공 시 JSON)
```json
{
  "recommended_city": "제주",
  "weather": "...",
  "events": ["...", "..."],
  "reason": "...",
  "restaurants": [
    {"name": "...", "address": "...", "category": "...", "url": "..."}
  ]
}
```
화면에는 추천 지역/이유, 날씨, 행사 목록, 맛집 카드 리스트 순으로 표시한다.

**실패 처리 (3가지 모두 구현)**

| 상황 | 감지 위치 | 사용자 안내 |
|---|---|---|
| 빈 입력(날짜 미입력) | 프론트(JS), fetch 호출 전 | "여행 날짜를 선택해주세요" 인라인 메시지, API 호출 자체를 막음 |
| API 오류(4xx/5xx) | 프론트, fetch 응답 status 확인 | "추천을 가져오는 데 실패했어요. 잠시 후 다시 시도해주세요" |
| 지연/타임아웃 | 프론트, `AbortController`로 12초 제한 | "응답이 지연되고 있어요. 잠시 후 다시 시도해주세요" |

백엔드에서도 OpenAI/Kakao 호출 실패를 각각 `try/except`로 잡아 맛집 없이도(또는 fallback
문구로) 응답을 내려보내, 프론트가 항상 유효한 JSON을 받도록 한다(A1-2 미션의 에러 처리
정책을 웹 서비스 구조에 맞게 재사용).

## 6. 기술 스택

- 프론트엔드: 바닐라 HTML/CSS/JavaScript (프레임워크 없음)
- 백엔드: Vercel Serverless Functions (Python, `api/recommend.py`)
- 외부 API: OpenAI(추천/이유 생성), Kakao Local(맛집 검색) — A1-2에서 이미 검증된 조합 재사용
- 배포: Vercel (GitHub 저장소 연동 자동 배포)
