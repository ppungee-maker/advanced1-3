[project]
name = "advanced1-3-ai-travel-recommend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "openai>=1.30.0",
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "api.recommend:handler"
